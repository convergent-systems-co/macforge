# macheim starter Brewfile.
# Refresh this with `brew bundle dump --describe --file=Brewfile` after the
# host machine has the right set of tools installed.

# Taps
tap "homebrew/cask-fonts"

# Formulae
brew "gh"
brew "git"
brew "jq"
brew "ripgrep"
brew "yq"

# Casks
cask "ghostty"
cask "rectangle"

# Mac App Store (requires `brew install mas` first)
# mas "Xcode", id: 497799835
